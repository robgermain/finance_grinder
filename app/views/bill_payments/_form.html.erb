<%= form_for(bill_payment) do |f| %>
  <% if bill_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_payment.errors.count, "error") %> prohibited this bill_payment from being saved:</h2>

      <ul>
      <% bill_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount_paid %>
    <%= f.text_field :amount_paid %>
  </div>

  <div class="field">
    <%= f.label :payment_date %>
    <%= f.text_field :payment_date %>
  </div>

  <div class="field">
    <%= f.label :confirmation_number %>
    <%= f.text_field :confirmation_number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
