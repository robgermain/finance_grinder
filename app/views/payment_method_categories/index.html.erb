
<table>
    <thead>
        <tr>
            <th scope="col">&nbsp;</th>
            <th scope="col">Name</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        <% @payment_method_categories.each do |payment_method_category| %>
            <tr>
                <td>x.</td>
                <td><%= link_to payment_method_category.name, payment_method_category_path(path) %></td>
                <td>
                    <%= link_to 'Edit', edit_payment_method_category_path(payment_method_category) %>
                    <%= link_to 'Delete', payment_method_category_path(payment_method_category), method: :delete, data: {confirm: "Are you sure?"} %></td>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'Add Payment Method Category', new_payment_method_category_path %>
