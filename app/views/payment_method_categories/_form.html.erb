<%= form_for(payment_method_category) do |f| %>
  <% if payment_method_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_method_category.errors.count, "error") %> prohibited this payment_method_category from being saved:</h2>

      <ul>
      <% payment_method_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
